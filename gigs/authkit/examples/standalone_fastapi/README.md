# Standalone FastAPI example

Install the package with its FastAPI extra, set `PYTHONPATH=src`, and run:

```text
uvicorn main:app --app-dir examples/standalone_fastapi
```

This example uses an in-memory repository and explicitly enabled memory
sessions. It is for demonstration only.
